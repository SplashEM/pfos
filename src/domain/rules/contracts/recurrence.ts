/**
 * How often an authored rule recurs (Decision 074).
 *
 * V1 supports only MONTHLY. No additional recurrence may be introduced without
 * an accepted specification.
 */
export type Recurrence = 'MONTHLY';

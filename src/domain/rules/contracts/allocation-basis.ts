/**
 * Which starting amount the Allocation Engine uses (Decision 074).
 *
 * `allocationBasis` identifies the starting amount. It does not carry or
 * calculate that amount.
 */
export type AllocationBasis = 'NET_AMOUNT' | 'ELIGIBLE_AMOUNT';

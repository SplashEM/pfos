/**
 * The authored income basis of an obligation (Decision 074).
 *
 * IncomeBasis is a separate selector from AllocationBasis. V1 exposes only
 * NET_DEPOSITED.
 */
export type IncomeBasis = 'NET_DEPOSITED';
